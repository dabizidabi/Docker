docker run -it --name Test -h Test ubuntu bash

#in docker VM
apt update
apt-get install cowsay
ln -s /usr/games/cowsay /usr/bin/cowsay
cowsay "OLOLO"

#push in docker hub
docker push dabizidabi/testkontainer:beta
